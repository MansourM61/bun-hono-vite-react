{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build --minify ./src/index.ts --outdir ./dist --target bun",
    "serve": "bun run dist/index.js"
  },
  "dependencies": {
    "hono": "^4.11.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
